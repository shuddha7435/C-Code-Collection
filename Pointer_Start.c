#include <stdio.h>

int main() {
    int x = 8;
    int y = 10;

    printf("Value of x is %d\n", x);
    printf("Address of x is %p\n", &x);
    printf("Value of y is %d\n", y);
    printf("Address of y is %p\n", &y);
    return 0;
}
